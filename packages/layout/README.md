# `@pptkit/layout`

Layout resolution primitives for PPTKit.

## Current Status

This package currently provides a placeholder layout pass so the workspace can validate package boundaries and downstream imports.

